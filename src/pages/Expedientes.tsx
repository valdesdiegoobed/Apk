import { Add, Search } from '@mui/icons-material';
import { Box, Button, Card, CardContent, Chip, InputAdornment, Stack, TextField, Typography } from '@mui/material';
import { Link } from 'react-router-dom';
import { expedientesDemo } from '../data';

export function Component() {
  return <Stack spacing={3}><Box><Typography variant="h4" sx={{ fontWeight: 700 }}>Expedientes privados</Typography><Typography color="text.secondary">Primera versión funcional con datos ficticios para validar el flujo antes de conectar Firebase en producción.</Typography></Box><Stack direction="row" spacing={2}><TextField fullWidth placeholder="Buscar por cliente, folio o responsable" slotProps={{input:{startAdornment:<InputAdornment position="start"><Search /></InputAdornment>}}}/><Button variant="contained" startIcon={<Add />}>Nuevo ficticio</Button></Stack><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', md: 'repeat(3, 1fr)' }, gap: 2 }}>{expedientesDemo.map((expediente)=><Box key={expediente.id}><Card sx={{height:'100%'}}><CardContent><Stack spacing={1.5}><Stack direction="row" sx={{ justifyContent: 'space-between' }}><Typography sx={{ fontWeight: 700 }}>{expediente.id}</Typography><Chip label={expediente.estado} color={expediente.estado==='Activo'?'success':expediente.estado==='En revisión'?'warning':'default'} size="small"/></Stack><Typography variant="h6">{expediente.cliente}</Typography><Typography color="text.secondary">{expediente.categoria} · {expediente.documentos} documentos</Typography><Typography variant="body2">Responsable: {expediente.responsable}</Typography><Typography variant="body2">Actualizado: {expediente.ultimaActualizacion}</Typography><Button component={Link} to={`/expedientes/${expediente.id}`} variant="outlined">Ver detalle</Button></Stack></CardContent></Card></Box>)}</Box></Stack>;
}
